export component SettingsWindow inherits Window {
    preferred-width: 800px;
    preferred-height: 600px;
    title: "Keystroke - " + @tr("Settings");

    Text {
        font-size: 20px;
        text: "Work in progress...";
    }
}
